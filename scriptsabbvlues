-- [GROK-RAW] Steal a Brainrot Speed Bypass UI - LinearVelocity Version (2026 Delta Compatible)

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local root = character:WaitForChild("HumanoidRootPart")

local speedEnabled = false
local normalSpeed = 50
local stealingSpeed = 85
local isStealing = false
local lv = nil

-- GUI (same clean dark UI)
local screenGui = Instance.new("ScreenGui")
screenGui.ResetOnSpawn = false
screenGui.Parent = player:WaitForChild("PlayerGui")

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 320, 0, 300)
mainFrame.Position = UDim2.new(0.5, -160, 0.4, 0)
mainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 40)
title.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
title.Text = "Steal a Brainrot Speed Bypass [Delta 2026]"
title.TextColor3 = Color3.new(1,1,1)
title.TextScaled = true
title.Parent = mainFrame

-- Normal speed
local normalLabel = Instance.new("TextLabel")
normalLabel.Size = UDim2.new(0.6, 0, 0, 30)
normalLabel.Position = UDim2.new(0.05, 0, 0, 55)
normalLabel.BackgroundTransparency = 1
normalLabel.Text = "Normal Speed:"
normalLabel.TextColor3 = Color3.new(1,1,1)
normalLabel.TextXAlignment = Enum.TextXAlignment.Left
normalLabel.Parent = mainFrame

local normalBox = Instance.new("TextBox")
normalBox.Size = UDim2.new(0.3, 0, 0, 30)
normalBox.Position = UDim2.new(0.65, 0, 0, 55)
normalBox.BackgroundColor3 = Color3.fromRGB(40,40,40)
normalBox.Text = tostring(normalSpeed)
normalBox.TextColor3 = Color3.new(1,1,1)
normalBox.Parent = mainFrame

-- Stealing speed
local stealLabel = Instance.new("TextLabel")
stealLabel.Size = UDim2.new(0.6, 0, 0, 30)
stealLabel.Position = UDim2.new(0.05, 0, 0, 95)
stealLabel.BackgroundTransparency = 1
stealLabel.Text = "Stealing Speed:"
stealLabel.TextColor3 = Color3.new(1,1,1)
stealLabel.TextXAlignment = Enum.TextXAlignment.Left
stealLabel.Parent = mainFrame

local stealBox = Instance.new("TextBox")
stealBox.Size = UDim2.new(0.3, 0, 0, 30)
stealBox.Position = UDim2.new(0.65, 0, 0, 95)
stealBox.BackgroundColor3 = Color3.fromRGB(40,40,40)
stealBox.Text = tostring(stealingSpeed)
stealBox.TextColor3 = Color3.new(1,1,1)
stealBox.Parent = mainFrame

local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0.9, 0, 0, 50)
toggleBtn.Position = UDim2.new(0.05, 0, 0, 140)
toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
toggleBtn.Text = "ENABLE SPEED BYPASS"
toggleBtn.TextColor3 = Color3.new(1,1,1)
toggleBtn.TextScaled = true
toggleBtn.Parent = mainFrame

local status = Instance.new("TextLabel")
status.Size = UDim2.new(0.9, 0, 0, 40)
status.Position = UDim2.new(0.05, 0, 0, 200)
status.BackgroundTransparency = 1
status.Text = "Status: Disabled"
status.TextColor3 = Color3.fromRGB(255, 80, 80)
status.TextScaled = true
status.Parent = mainFrame

local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 30, 0, 30)
closeBtn.Position = UDim2.new(1, -35, 0, 5)
closeBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.new(1,1,1)
closeBtn.Parent = mainFrame

-- Create LinearVelocity bypass
local function createBypass()
    if lv then lv:Destroy() end
    lv = Instance.new("LinearVelocity")
    lv.Name = "SpeedBypassLV"
    lv.Attachment0 = Instance.new("Attachment", root)
    lv.MaxForce = 50000
    lv.VectorVelocity = Vector3.new(0,0,0)
    lv.Parent = root
end

local function updateSpeed()
    if not speedEnabled or not lv then return end

    local current = isStealing and stealingSpeed or normalSpeed
    local move = humanoid.MoveDirection * current
    lv.VectorVelocity = Vector3.new(move.X * current, 0, move.Z * current)
end

local function checkStealing()
    -- Basic detection (adjust if needed)
    isStealing = false
    if character:FindFirstChildWhichIsA("Tool") or player.Backpack:FindFirstChildWhichIsA("Tool") then
        isStealing = true
    end
end

RunService.Heartbeat:Connect(function()
    if speedEnabled and character and root then
        checkStealing()
        updateSpeed()
        status.Text = "Status: ENABLED | " .. (isStealing and "STEALING MODE" or "NORMAL MODE")
        status.TextColor3 = Color3.fromRGB(0, 255, 100)
    end
end)

-- Events
normalBox.FocusLost:Connect(function() normalSpeed = tonumber(normalBox.Text) or 50; normalBox.Text = normalSpeed end)
stealBox.FocusLost:Connect(function() stealingSpeed = tonumber(stealBox.Text) or 85; stealBox.Text = stealingSpeed end)

toggleBtn.MouseButton1Click:Connect(function()
    speedEnabled = not speedEnabled
    if speedEnabled then
        createBypass()
        toggleBtn.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
        toggleBtn.Text = "DISABLE SPEED BYPASS"
    else
        if lv then lv:Destroy() end
        lv = nil
        toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
        toggleBtn.Text = "ENABLE SPEED BYPASS"
        status.Text = "Status: Disabled"
        status.TextColor3 = Color3.fromRGB(255, 80, 80)
    end
end)

closeBtn.MouseButton1Click:Connect(function() screenGui:Destroy() end)

player.CharacterAdded:Connect(function(newChar)
    character = newChar
    humanoid = newChar:WaitForChild("Humanoid")
    root = newChar:WaitForChild("HumanoidRootPart")
    if speedEnabled then createBypass() end
end)

print("[GROK-RAW] LinearVelocity Speed Bypass UI loaded for Delta Executor.")
